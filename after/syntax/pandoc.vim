" Override some colors of pandoc syntax highlighting
" Note:
" - since color groups are not defined using hi link default <from> <to>
"   we can't override in ~/.vimrc and need to do it here.
hi! link PandocTitleBlockTitle Identifier

